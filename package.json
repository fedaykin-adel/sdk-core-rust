{
  "name": "shaayud",
  "private": true,
  "devDependencies": {
    "typescript": "^5.x",
    "@napi-rs/cli": "^2.x"
  },
  "workspaces": [
    "packages/sdk-*"
  ],
  "scripts": {
    "build:ffi": "cd packages/ffi && cargo clean && npm run pack",
    "build:web": "cd packages/sdk-web && npm run pack",
    "build:node": "cd packages/sdk-node && npm run pack",
    "run:backend": "cd packages/backend && cargo build && cargo run",
    "build:core":"cd packages/core && cargo build",
    
    "db":"docker compose up -d"
  }
    
}